# musializer

## running locally 

to run this application locally make sure you have the latest version of python

and also make sure to install the `uv` an a package manager for the python 

after installing make sure to clone this repo 

and run 
` uv sync `
to install the packages 

and then run 
` uv run main.py `

